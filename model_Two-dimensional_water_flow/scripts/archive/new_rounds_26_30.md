---

## 第 26 轮：外部审查（REVIEW_REPORT.md）问题清单逐项核实
- **审查对象**：评审报告 P1-1 / P1-2 / P2 / P3 清单。
- **核实结论**（诚实核对后被证实的问题）
  1. **P1-1 成立（真 bug）**：batch_sim_drops 第 71 行对 C0 峰值重新抽了一次初始粒子（与真正被平流的初始群不是同一次实现），导致阈值系统性错位 ±4%。**修复**：保存各候选的实际 pos0 分组，用同一次实现计算 C0；修复后重跑优化，并新增多种子（seed 1/2）稳健性核验（见第 27 轮）。
  2. **P1-2 成立（口径错误）**：Open-Meteo 原始单位是 km/h（须 /3.6）；按本项目自己下载的数据复核：年均 2.87 m/s、静风(<0.5 m/s) 仅 1.5%、主导扇区 N 26.6%、全年圆形均值 59.3°、夏季(6-8月) 2.75 m/s/142°——与文档沿用的“站点统计 1.8–2.3 m/s、静风 21.8%”相差一个数量级。**处理**：§6 全面改写：以本项目数据为准，把站点口径标为“对照源”，撤销“21.8% 静风”论证，改为 1.5%/6.9%（低风）并注明口径差异来源（ERA5 再分析网格 vs 站点观测）。
- **P2-1 成立**：fig01 标题确为“成都东湖”（笔误）——已改“武汉东湖”并重跑生成图件。
- **P2-2 成立**：fig05 第三面板是占位文字——已改为 Manning-覆盖柱状图（数据来自 12 h 自旋实测）并重跑。
- **P2-3 成立（现已过时）**：文献综述 A1 与文末下载表矛盾；**用户已补充下载全文** —— 已更新为“已下载（用户补充）：ijgi-09-00094.pdf”，并新增 §18 全文对齐分析。
- **P2-4 成立**：“最大 5.8 m”是标定目标；场上实际最大 5.47 m（新标定 4.75 目标 → 实际 4.64 m）。—— §5.2/导读均已改为“目标/实际”双口径。
- **P2-5 成立（表述精度）**：体积守恒 <1e-15 严格来说是“η 方程（自由面）积分守恒”，底摩擦在 η 解算之后施加，未再回代重解——已把表述精确化（§10 注）。“差异可忽略”的结论维持。
- **P2-6 成立**：§16“scripts/01_fetch…”不存在——已改为实际脚本名单。
- **P2-7 成立（小）**：“互差<1%”→实测前五得分 0.052–0.053（0.1 个百分点、相对约 2%）——已改“0.1 个百分点内”。
- **P3 项**：1) gitignore（根`.gitignore`已有 `**/data/`、`**/raw/`、`__pycache__/`——raw zip 本就不入库；已把 DATASETS“均随项目提交”改为与事实一致的表述）；2) __pycache__ 已忽略（无需改）；3) 绝对路径/字体回退：fetch_wind.py、07 等已用相对路径；domain.py 字体改为多路径回退；4) use_adv 默认改为 True；5) 死代码已删；6) 调试脚本归档到 scripts/archive/；7) density 重归一化已记入文档与 docstring；8) face_depths 已加水深下限防御。

## 第 27 轮：修复后的优化器稳健性复核（种子稳定性）
- **审查对象**：修复 P1-1 后，Top-K 投放点是否对随机种子稳健（这是“决策稳健”卖点的必要证据）。
- **验证**：seed=1、seed=2 重跑批量优化（5 成员 × 792 候选 × 600 粒子），与 seed=7（主报告）对比 Top-5。
- **结论**：3 个种子下 Top-5 均落在同一区域（斑块上风侧 ±600 m 内），期望保护比例 0.052–0.053 基本一致 → **修复后优化结果对抽样种子稳健**（详细数字见 §8.2 与 04b 输出）。

## 第 28 轮：论文全文（MIKE21 东湖）驱动的参数与边界对齐
- **审查对象**：已下载论文全文的技术细节能否直接改进本模型。
- **已落地的对齐**：① 水深标定目标改为论文实测值（平均 2.21 m、最大 4.75 m）；② Manning 默认 0.022→0.0238（其校准 M=42，初值 M=35）；③ 涡黏常数 ν=0.5→Smagorinsky Cs=0.28（其 Smagorinsky 默认 0.28–0.32、校准 0.29）；④ 新增 Cd 风速函数（Wu 1980 显式化）并实测（−14% 流速）；⑤ 新增源/汇开边界机制（对应其 4 个开边界：东湖港/东沙湖港进水、新沟/九峰港出水），做了 ±5 m³/s 敏感性（局地射流型，全域 +2%）；⑥ 新沟验证点口径（水位<15%、流速<25%）记录为待复现项。
- **结论与遗留**：模型与东湖 MIKE21 基线的物理口径基本对齐；仍差：实测验证数据、绝对水位基准、非结构网格、点源污染位置——全部列入 §18.4 诚实清单。

## 第 29 轮：同期重演（2017-11-15 — 2017-12-17 时变风场）
- **审查对象**：论文模拟时段与本模型“稳态流场”假设的差距。
- **验证**：Open-Meteo 2017 年逐时风驱动、dt=40 s 全时段积分（scripts/10_run_2017period.py），输出逐时流场指标。
- **结论**：（回填结果）——用于评估“单个稳态流场支撑 2 h 投放窗”的近似是否成立（若 6h 内流场变化超阈值，则改为逐时段推演）。

## 第 30 轮：文档与工程整洁终审
- 全文核对文档-代码-图件一致性（图件全部重生成；数字表格全部用新参数集的实测值）；调试/补丁脚本归入 scripts/archive/；README/迭代日志/架构文档三处口径统一；新增“与 MIKE21 论文对齐”作为第 18 节，把“尚未尝试”清单中的 Smagorinsky/开边界/Cd 函数三项从“未尝试”移到“已实现”；剩余未尝试项（实测等深、3D、PINN、多点协同、非结构网格等）如实保留。