# 07 · 优化实验流程记录（2026-08-24，V2.7 轮）

> **目的**：把本轮全部"文献 → 改法 → 实验 → 结论"的流程记录下来，保证每条结论都能追溯到代码、命令与数字。
> **原则**：① 每个实验先写假设再写结果；② 所有对比使用**同一评分器**；③ 所有"更好"的结论必须同时给出效应量与不确定性；④ 失败/无差异的结果同样记录。

---

## 0. 环境与代码基线

| 项 | 值 |
|---|---|
| 机器 | Windows 11, CPU 推理/训练（无 GPU） |
| 环境 | Python 3.12, torch 2.13.0+cpu, ortools 9.15.6755, numpy/matplotlib/pypdf |
| 代码基线 | git `feature/pathplanning-optimization`；基线提交 713d7fe（V2.6 全量）；修复提交 65fb9a4（F2–F6/F8/F9 + 实验基础设施） |
| 数据 | 东湖 OSM + Open-Meteo 2928 h 风场（见 `data/`）；v3.0 任务来自 `model_Two-dimensional_water_flow/data/processed/opt_result.npz`（792 候选点/scores/斑块 1.41 km²）与降解模型参数（k_base=0.25 h⁻¹ 等） |
| 随机性 | 训练 seed 0；测试场景 seed 9000–9015（东湖）/ 9000–9007（v3.0）；固定种子保证可复现 |

**实验流概述**（每轮训练 ~25–30 min CPU；评估复用 `exp_v27.py` 统一输出）：

```
Exp1  修复验证: F3(独立时钟)/F4(第demand次完成时刻)/F2(OR-Tools状态披露)/F5(λ敏感性)/F6(默认超参)
Exp2  重训A (V2.7 基线, 独立时钟, 无边特征) → 东湖16场景 + 强风压力测试
Exp3  重训B (+ANE-lite边特征 + tanh截断 + −β·log(T) 解码先验) → 同上, 与A对比
Exp4  推理×8增广 (风逆转+镜像) 在 A/B 上的增益
Exp5  重训C (v3.0 水流任务, t_eff 治理生效目标) → v3.0 16场景对比 + 旧策略迁移对照
Exp6  λ敏感性 (F5 实证) + 采样数敏感性
Exp7  OOD 诚实重测 (OR-Tools 状态披露, F4 修正后语义)
Exp8  锚定训练 (真 POMO, 若 Exp3 增益不足则加验)
```

## 1. 实验过程与结果

### Exp1 修复验证（已完成，证据在案）

| 缺陷 | 修复 | 验证证据 |
|---|---|---|
| F3 共享时钟 | `train.rollouts` tnow→(B2,m) | 修复前：三机真实结束 [12.98,11.55,13.58] min 但 rollout 记 38.11（≈2.8×）；修复后 tnow 逐机推进，时间掩码按 (B2,m,n) 广播 |
| F4 完成时刻 | `env.objective_times` visit_times 列表取第 demand 次 | 构造 demand=2、两次访问 t≈3.0/6.27 → t_complete=6.268（修复前得 3.0） |
| F2 静默回退 | `ortools_solve` 返回 (routes, status) | OOD 9/9 实例 status="fallback" 显式披露（旧表两列逐行相同的原因即在此） |
| F5 λ 缺失 | `ablation.lambda_sens` 实现（评估端重排+固定决策后悔） | 见 Exp6 |
| F6 默认超参 | `--d 128` + ckpt_config.json | `load_policy` 按配置加载，无需猜测结构 |
| F8/F9 | replan 死代码删除；"nearest"→greedy_replan | 重规划三策略标签真实化（见 Exp 附） |

### Exp2 重训 A（V2.7 基线：独立时钟，无边特征）

**命令**：`python train.py --steps 1200 --d 128 --out outA`（1436 s，val 终点 20.93；V2.6 旧训练 21.43——时钟修复带来小幅收敛增益）。

**东湖 16 场景**（同一评分器；不可行重罚协议 1e4 不触发——全部计划能量可行）：

| 方法 | rwt | makespan | served | obj | 求解时间 |
|---|---|---|---|---|---|
| 贪心 | 33.46 | 42.01 | 10/10 | 37.45 | <0.01 s |
| 贪心+LS | 16.74 | 31.61 | 10/10 | 18.32 | 0.04 s |
| OR-Tools | 19.83 | 14.52 | 10/10 | 20.56 | 5.01 s |
| **RL(采样32)** | **15.51** | 22.59 | 10/10 | 16.64 | **0.05 s** |
| RL+LS | 15.18 | 21.09 | 10/10 | 16.23 | 0.04 s |

解读：与 V2.6 报告表（RL 15.83/16.60）几乎一致（±0.3）；RL 对贪心+LS 的优势：rwt −7.4%、obj −9.2%；对 OR-Tools：rwt −21.8%、速度 100×。**时钟修复不改变东湖结论**（东湖场景时间窗宽松，共享时钟惩罚小）——预期内：坏时钟主要影响大时域/紧窗场景。

**强风 10 m/s 压力测试（关键发现，协议修正后）**：

| 规划方式 | 按真实强风成本评分 | 差距 |
|---|---|---|
| 风感知规划（A） | **37.10** | — |
| 欧氏规划（按欧氏 T 生成） | 1035.61 | 风感知优 **96.4%** |

> **结论反转（重要）**：旧消融（V2.6，report 03 §5.3）报告"风感知 vs 欧氏仅差 ~1.1%"——本次以修正协议重测发现该结论是**评分伪影**：旧协议不惩罚能量不可行解，欧氏规划在强风下"作弊"（其路线在真实强风矩阵上能量超标，但评分照常）。加入不可行重罚后：欧氏规划全部能量不可行（+1e4 罚）、风感知规划保持可行，差距 96.4%。**更正后的结论：风修正矩阵的价值主要在"可行性"（掩码），而非"偏好"（编码器表征）**——RRNCO 式"边进表征"的必要性需重新定位（见 Exp3）。

**v3.0 水流任务 16 场景（零样本迁移，t_eff 目标）**：

| 方法 | rwt | rwt_eff | makespan | obj |
|---|---|---|---|---|
| 贪心 | 325.26 | 2891.73 | 96.90 | 3917.72 |
| 贪心+LS | 80.23 | 2646.70 | 65.79 | 3141.25 |
| OR-Tools | 287.29 | 2853.75 | 73.29 | 3818.71（**fallback 7/16**——多访问语义 OR-Tools 无法表达） |
| **RL(采样16)** | **96.43** | 2662.90 | 40.85 | 3185.11 |
| RL+LS | **41.63** | 2608.10 | 32.15 | **3021.17** |

解读：A（从未见过 v3.0 任务分布）零样本迁移后仍大幅优于贪心（rwt 3.4×）与 OR-Tools（obj −17%、速度 100×），说明 V2.6 结构对"投放点+多访问+治理时滞"有跨分布泛化；RL+LS 综合最佳。**但这只是迁移下限**——Exp5 训练 C（v3.0 分布内训练）将给出上限。

