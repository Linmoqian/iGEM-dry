# CMADRE 模型性能迭代记录

起始日期：2026-08-11  
开发分支：`codex/model-v3-performance`  
服务器项目：`/home/linux/igem-cmadre`  
原则：所有方案先由训练/验证或重复分组外层折选择；历史 locked test 只用于最终审计和已明确标注的事后诊断，不用于反复调参。

## 0. 当前基线与问题定义

当前基线为 stacking v2：XGBoost AFT、CatBoost quantile、TabM-inspired censored MiniEnsemble，经来源一致的 OOF 非负 stacking、CQR 和 Mahalanobis OOD 组成。

| 任务 | v2 test `log1p MAE` | Spearman | R² | 关键问题 |
|---|---:|---:|---:|---|
| total MC core / source OOD | 0.3488 | 0.3190 | -0.0107 | 99.3% OOD；高值严重低估 |
| MC-LR static / source OOD | 0.1318 | 0.0831 | -3.0590 | 只有 134 条精确测试标签；100% OOD；几乎无排序能力 |
| MC-LR core / waterbody OOD | 0.2972 | 0.5091 | -0.0045 | 少数极端值使 MAE 达 32.07 µg/L |

事后误差诊断进一步显示：

- total MC 测试 top 1%（19 条）真实浓度中位区域远高于模型预测，其 MAE 为 60.14 µg/L、`log1p MAE` 为 3.61；
- MC-LR core 的 7,700、3,700、3,291、2,066 µg/L 样本分别只预测约 0.94、0.59、10.42、14.32 µg/L；
- 这些 MC-LR 极端样本多数没有被特征 OOD 检出，说明主要问题是条件尾部/概念不足，而非单纯边缘特征距离；
- 在完整动态表精确标签中，Chla 与 total MC 的 Spearman 约 0.44，蓝藻细胞数与 MC-LR 的 Spearman 约 0.60；高值组的 Chla/蓝藻量显著更高。

## 1. 改进优先级判断

### 1.1 第一优先级：评价协议与数据表征

改进点：

1. 保留历史 test，不再根据它直接选择超参数；建立 repeated source/waterbody OOD 开发评价。
2. 将误差按来源、标签分位区间、删失状态和 OOD 分解，新增 tail 指标。
3. 将 `core_field` 与可部署的 `bloom_augmented` 分开建模，禁止用同步 bloom proxy 的提升冒充 core 模型提升。
4. 核验极端 MC-LR 的单位、样品矩阵、方法和重复关系；核验前不自动删除，也不把其主导的原尺度指标隐藏掉。

判断：当前瓶颈首先是域偏移、标签尾部和有效信号面板，不是 GPU 或网络容量。数据与评价的投入优先级高于更大的 Transformer。

### 1.2 第二优先级：尾部感知且删失一致的专家

计划改进点：

1. 增加 XGBoost quantile 专家，与 AFT 的固定分布尾部形成互补；
2. 研究三状态模型：未检出/普通浓度/高浓度，使用训练折定义尾阈值；
3. 高值专家只由训练折精确高值训练，以概率 gate 融合，避免全局抬高低浓度预测；
4. stacking 升级为带正则的 simplex 优化，并增加删失区间损失/尾部损失消融；
5. 对 AFT 分布与 scale 做内部折选择，不继续固定单一 normal-scale=1。

### 1.3 第三优先级：域稳健和迁移

计划改进点：

1. repeated source folds 下比较来源均衡、GroupDRO 和稳健最差组目标；
2. 使用中国无标签协变量训练域分类器和可解释 OOD，而不是生成伪标签；
3. total MC 预训练→MC-LR 微调仅作为消融，必须同时改善 MC-LR 平均和最差来源；
4. 有东湖标签后再做本地校准和 weighted/Mondrian conformal。

## 2. 迭代 I0：同步藻华面板诊断

### 方案

不修改模型，仅把面板从 `core_field` 改为 `bloom_augmented`，增加 `chlorophyll_a_ug_l` 与 `cyanobacteria_cells_ml`，保持原 v2 切分、三个专家、训练预算和 stacking 规则不变。目的不是直接替换 core，而是判断尾部识别是否缺少藻华状态信号。

### total MC 结果

服务器运行：`runs/total_mc_bloom_augmented_v2/`。

| 指标 | core v2 | bloom I0 | 变化 |
|---|---:|---:|---:|
| test `log1p MAE` | 0.3488 | 0.3549 | 退化 0.0061 |
| Spearman | 0.3190 | 0.3963 | 改善 0.0773 |
| factor-of-2 | 0.3811 | 0.3412 | 退化 |
| 区间距离 MAE (µg/L) | 0.5988 | 0.6015 | 近似持平、略退化 |
| 95% 高值组 Spearman | 0.2366 | 0.3630 | 明显改善 |
| OOD 比例 | 0.9930 | 1.0000 | 退化 |

stacking 权重由 `(XGB=0, Cat=0.8304, TabM=0.1696)` 变为 `(0, 0.6457, 0.3543)`。

### 反思

藻华代理提高了整体和高值组排序，但没有提高浓度幅度，说明新增特征包含尾部识别信号，而当前中位损失、删失中点 stacking 和全局专家组合没有把该信号转化成足够高的尾部预测。`bloom_augmented` 不应直接替代 core；下一轮应增加 tail-aware objective/gate，并对缺失模式引起的 OOD 上升进行处理。

### MC-LR 结果

服务器运行：`runs/mc_lr_bloom_augmented_v2/`。

| 指标 | core v2 | bloom I0 | 变化 |
|---|---:|---:|---:|
| test `log1p MAE` | 0.2972 | 0.2634 | 改善约 11.4% |
| 原尺度 MAE (µg/L) | 32.0704 | 30.1193 | 改善约 6.1% |
| MedianAE (µg/L) | 0.1029 | 0.0800 | 改善约 22.3% |
| Spearman | 0.5091 | 0.5300 | 改善 |
| R² | -0.0045 | 0.0336 | 首次转正 |
| factor-of-2 | 0.5408 | 0.5612 | 改善 |
| 最差来源 `log1p MAE` | 0.3339 | 0.2939 | 改善约 12.0% |

点预测的多个维度一致改善，证明 bloom 信号对 MC-LR core 有实质价值。但 TabM 的 q90 在部分样本发生尺度爆炸，使 ensemble 平均区间宽度达到约 `2.65e17 µg/L`。该运行只能证明点预测方向，不能作为概率模型候选。

### 决策

保留 `bloom_augmented` 作为“有同步藻华传感器时”的高性能面板，不替代 core fallback。进入 I1 修复 TabM 潜变量尺度与集成区间目标，然后重新训练。

## 3. 迭代 I1：TabM 不确定度稳定化

### 假设与改进点

MC-LR bloom 中 TabM 的位置预测有价值，但 exact-only 数据对尺度头约束不足；`softplus(raw_scale)` 无上界，少数 member 可输出极大 `sigma`，而 q50-only stacking 完全看不到 q90 爆炸。第一步先修复基模型的统计病态，再改 stacking。

实现：

- 新训练默认把 `Z=log1p(Y)` 的 `sigma` 限制在 `[0.03,3.0]`；
- 使用 sigmoid 有界映射，保持梯度连续；
- 旧 v2 制品参数中没有 `max_scale`，回载时继续走原 softplus 路径，保证历史制品复现不被破坏；
- 增加极端 scale bias 单元测试，确保输出有限且不超过配置上限。

本地与服务器测试：14 passed。服务器运行：`runs/mc_lr_bloom_bounded_tabm_i1/`。

| 指标 | I0 无界 scale | I1 有界 scale | 判断 |
|---|---:|---:|---|
| ensemble `log1p MAE` | 0.2634 | 0.2649 | 轻微退化 |
| 原尺度 MAE (µg/L) | 30.1193 | 29.6059 | 改善 |
| R² | 0.0336 | 0.0476 | 改善 |
| TabM 平均区间宽度 | `7.07e17` | 198.73 | 消除数值爆炸，但仍过宽 |
| ensemble 平均区间宽度 | `2.65e17` | 77.73 | 消除数值爆炸，但仍过宽 |
| q10–q90 coverage | 0.7289 | 0.7271 | 未改善 |

反思：有界 scale 是必须保留的安全修复，但单独限制基模型不足以得到高质量区间。旧 stacker 只优化 q50，仍给区间过宽的 TabM 约 33% 权重；因此下一轮必须让集成目标看到 q10/q90。

## 4. 迭代 I2：分布感知 stacking 与尾部 XGBoost 专家

### 假设与改进点

1. q50-only NNLS 无法约束区间质量；
2. CatBoost 分位数是中点代理，XGBoost 3.x 的 `reg:quantileerror` 可提供不同的树分位数归纳偏置；
3. 训练分布由大量低值主导，需要一个只在训练折计算尾阈值的高值加权专家，为 stacking 提供尾部方向，但不能用测试分位数构造权重。

实现：

- 新增 `xgb_quantile`：q10/q50/q90 三个 GPU quantile booster；
- 新增 `xgb_quantile_tail`：在每个训练折内以精确标签 q90 定义尾部，按严重程度最高增加 4 倍训练权重；
- 新增 `distributional` stacking objective，同时优化：
  - q50 到精确/删失标签区间的 log 距离；
  - 精确标签 q10/q90 pinball loss；
  - 删失区间不相容惩罚；
  - 超过配置 log-width 的病态区间惩罚；
  - simplex 权重的 L2 稳定正则；
- 保留 `median_nnls` 默认路径和旧制品加载兼容；
- 新增 pathological q90 专家必须被降为近零权重的测试；
- 新增 `configs/mc_lr_bloom_v3.json` 作为 I2 正式实验配置。

本地检查：16 passed，ruff passed。服务器运行：`runs/mc_lr_bloom_distributional_i2/`。

| 指标 | core v2 | I1 | I2 | I2 相对 core |
|---|---:|---:|---:|---:|
| `log1p MAE` | 0.2972 | 0.2649 | 0.2615 | 改善约 12.0% |
| 原尺度 MAE (µg/L) | 32.0704 | 29.6059 | 29.0386 | 改善约 9.5% |
| Spearman | 0.5091 | 0.5272 | 0.5181 | 改善约 0.009 |
| R² | -0.0045 | 0.0476 | 0.0509 | 转正 |
| factor-of-2 | 0.5408 | 0.5408 | 0.6102 | 明显改善 |
| 最差来源 `log1p MAE` | 0.3339 | 0.2966 | 0.2863 | 改善约 14.3% |
| top 1% `log1p MAE` | 5.6981 | 3.6746 | 3.4896 | 改善约 38.8% |
| ensemble 区间宽度 | 2.267 | 77.73 | 41.61 | 相对 I1 收窄，但仍宽 |
| coverage | 0.7905 | 0.7271 | 0.7606 | 接近但低于 80% |

I2 权重：AFT 0.3522、tail quantile 0.4141、TabM 0.2337；普通 XGBoost quantile 与 CatBoost 为 0。普通 XGBoost quantile 在该历史 test 上单模型 `log1p MAE=0.2423`，优于 ensemble，但 OOF 分布目标未选择它。不能根据这个 test 事后切换，必须通过所有 outer folds 判断其稳定性。

### 反思与决策

I2 同时改善主体误差、最差来源、factor-of-2 和极端尾部，进入候选。但区间仍偏宽，且单模型/OOF 选择出现冲突。下一步不是继续看同一 test，而是运行完整 waterbody outer CV；同时把 q90/q95/q99 的误差与低估率写入正式 metrics。

## 5. 迭代 I3：完整非 IID 外层折评价

改进点：

- 新增 `scripts/run_outer_cv.py`，按全部 source/waterbody fold 和可配置重复种子顺序训练；
- 每折仍保持独立 validation、内部 OOF stacking、CQR 和 test；
- 输出逐折 CSV 和均值、标准差、最差折、权重及基模型指标；
- 正式 metrics 新增 exact-label q90/q95/q99 尾部 MAE、log MAE、中位预测和低估率；这些只作结果诊断，不进入训练阈值。

状态：代码已实现，待测试并在服务器比较 core 基线与 I2。

扩展计划：在 MC-LR waterbody CV 完成后，使用同一 I2 架构分别运行 total MC core、total MC bloom 和 MC-LR static 的完整 source CV。三个配置分别为 `total_mc_core_v3.json`、`total_mc_bloom_v3.json`、`mc_lr_static_v3.json`；只有跨全部来源的均值和最差折改善，才进入对应任务候选。

### MC-LR 五折结果

| 五折均值 | core I1 | bloom I2 | 相对变化 |
|---|---:|---:|---:|
| `log1p MAE` | 0.3321 ± 0.0644 | 0.2680 ± 0.0495 | 改善 19.3% |
| MedianAE (µg/L) | 0.1055 | 0.0824 | 改善 21.9% |
| Spearman | 0.5740 | 0.5865 | 改善 |
| factor-of-2 | 0.5678 | 0.6434 | 改善 13.3% |
| 来源宏平均 `log1p MAE` | 0.2307 | 0.1983 | 改善 14.0% |
| 最差来源 `log1p MAE` | 0.3681 | 0.2927 | 改善 20.5% |
| q90 tail `log1p MAE` | 1.9126 | 1.4264 | 改善 25.4% |
| q99 tail `log1p MAE` | 5.9544 | 3.6931 | 改善 38.0% |
| q10–q90 coverage | 0.8072 | 0.8213 | 接近名义 80% |
| 平均区间宽度 (µg/L) | 5.37 | 58.64 | 明显变宽 |

五个 test fold 的 `log1p MAE` 均改善，说明 I2 的点性能和尾部收益具有跨水体一致性。原尺度 MAE 均值由 60.41 变为 63.67、平均 R² 受 fold 4 的过预测影响而下降，说明区间/尾部专家仍可能在部分水体过度外推。

基模型五折均值：tail XGBoost quantile 0.2687、AFT 0.2746、普通 XGBoost quantile 0.2869、CatBoost 0.3000、TabM 0.3434。TabM 点误差持续最差但平均获得 22.3% stacking 权重，主要用于扩大覆盖。

### 决策

I2 的 bloom 面板和 tail quantile 专家确认保留。进入 I4 两个等切分消融：

1. `mc_lr_bloom_v4_tree_only.json`：删除 TabM，宽度阈值收紧至 log-width 3；
2. `mc_lr_bloom_v4_stable_tabm.json`：保留 TabM，但 `max_scale` 从 3.0 降至 1.5、宽度惩罚提高至 0.2。

选择标准依次为：五折 `log1p MAE`、最差来源、q99 tail、coverage 与宽度联合，而不是只追求最窄区间。

## 6. 数据语义审计：Uruguay `mcy` 的分析物边界

### 发现

Uruguay Río de la Plata 数据约占 MC-LR dynamic-ready 的 84%，并贡献绝大多数 >1000 µg/L 极端值。官方 Dryad 数据字典将 `mcy` 描述为 `total microcystin-LR concentration`，单位 µg/L；官方方法页又写明浓度由 `Microcystins-ADDA ELISA` 估计。ADDA ELISA 与 LC-MS/MS 的单一 MC-LR 定量不是同一观测模型，至少应视为“方法定义的 MC-LR/微囊藻毒素当量”，不能在不做敏感性分析的情况下与 congener-specific LC-MS/MS 真值完全等同。

### 改进点

- 不删除或改写任何原始/处理数据；
- 在模型加载层新增 `include_sources` / `exclude_sources`，形成可审计的数据视图，原文件 SHA-256 保持不变；
- 新增 `mc_lr_bloom_congener_sensitivity_v3.json`，暂时排除 Uruguay，评估模型结论对分析物定义的敏感性；
- 最终建议把 Uruguay 单列为 `mc_lr_equivalent_adda_elisa` 辅助任务，并与 LC-MS/MS MC-LR 使用独立头或独立校准器；重分类前需领域专家确认。

### 预期影响

排除 Uruguay 后 dynamic 样本会大幅减少，因此指标未必变好；该实验目标是提高结论有效性，而不是制造更漂亮的分数。如果性能下降，应优先补充真实 LC-MS/MS MC-LR + 同步水质数据，而不是重新混回语义不同的标签。

## 7. 后续迭代模板

每轮必须记录：

- 假设与改进点；
- 修改文件和配置；
- 训练/验证协议；
- 与固定基线的指标表；
- GPU 运行与制品路径；
- 是否保留、回退或进入下一轮；
- 失败原因和下一步反思。

## 8. I3 扩展实验：任务边界与数据域

所有结果均来自服务器 `/home/linux/igem-cmadre`、RTX 4090 CUDA 训练；各实验使用完整非 IID 外层折，未根据 test 结果调权。

### 8.1 total MC：core 与 bloom 的五折对照

| 五折均值 | core I2 | bloom I2 | 判断 |
|---|---:|---:|---|
| `log1p MAE` | 0.3789 | 0.3606 | 改善 4.8% |
| 原尺度 MAE (µg/L) | 1.6346 | 1.5773 | 改善 3.5% |
| MedianAE (µg/L) | 0.4216 | 0.3575 | 改善 15.2% |
| R² | -0.0232 | 0.0047 | 由负转正 |
| Spearman | 0.2487 | 0.3197 | 改善 0.0710 |
| factor-of-2 | 0.4524 | 0.4484 | 轻微退化 |
| 最差来源 `log1p MAE` | 0.6009 | 0.6026 | 基本持平 |
| q99 tail `log1p MAE` | 2.8056 | 2.7550 | 小幅改善 |
| q10–q90 coverage | 0.8407 | 0.7811 | 降低但接近名义 80% |
| 平均区间宽度 (µg/L) | 2.6701 | 2.7366 | 增加 2.5% |

结果路径：`runs/cv_summaries/total_core_i2_cv1/` 与 `runs/cv_summaries/total_bloom_i2_cv1/`。bloom 信号对 total MC 有稳定但有限的价值，因此保留高性能面板；来源间方差仍是主误差，不能靠增加模型复杂度掩盖。

### 8.2 MC-LR static 后备任务

`runs/cv_summaries/mclr_static_i2_cv1/` 使用配置定义的 4 个来源折：`log1p MAE=0.2315±0.1841`、Spearman 0.0573、coverage 0.5941。不同折的 test 行数从 102 到 3551，且每折几乎等价于单来源外推，方差过大。决策：只作为缺少同步水质输入时的低置信度后备模型，不能作为动态风险预警主模型。

### 8.3 分析物敏感性

排除 Uruguay ADDA-ELISA 后，严格分析物视图仅约 444 行；`runs/cv_summaries/mclr_congener_sensitivity_i3_cv1/` 的五折结果为：`log1p MAE=0.1425±0.0900`、MAE 0.2668 µg/L、Spearman 0.1685、R² -0.0572、coverage 0.7362、宽度 0.3806 µg/L。

改进点与反思：误差尺度明显缩小，证实原任务的极端误差受分析方法域强烈影响；但排序和 R² 仍弱，说明严格 congener-specific 数据不足。该结果不能与全数据 0.2680 直接比较，更不能解释为通用 MC-LR 模型已解决。下一批数据应优先收集分析方法明确的 LC-MS/MS MC-LR、同步藻华生物量和营养盐。

## 9. I4：专家方差消融

### 方案与改进点

- tree-only：移除五折点误差最差、却因宽区间获得权重的 TabM；
- stable-TabM：保留 TabM，但把 `max_scale` 从 3.0 收紧至 1.5，并提高宽度惩罚；
- 两者共享 I2 的数据、waterbody folds、种子、树模型预算和分布式 stacking，只改变待检验因素。

| 五折均值 | I2 | I4 tree-only | I4 stable-TabM |
|---|---:|---:|---:|
| `log1p MAE` | 0.2680 | **0.2592** | 0.2815 |
| 原尺度 MAE (µg/L) | 63.6653 | 64.3918 | 63.4333 |
| MedianAE (µg/L) | 0.0824 | **0.0586** | 0.1192 |
| R² | -0.3044 | -0.4033 | -0.2659 |
| Spearman | 0.5865 | 0.5877 | 0.5888 |
| factor-of-2 | 0.6434 | **0.6752** | 0.5735 |
| 最差来源 `log1p MAE` | 0.2927 | **0.2850** | 0.3070 |
| q99 tail `log1p MAE` | 3.6931 | **3.6581** | 3.6964 |
| coverage | 0.8213 | 0.8128 | 0.8220 |
| 平均区间宽度 (µg/L) | 58.6351 | 47.0007 | **41.5222** |

结果路径：`runs/cv_summaries/mclr_bloom_tree_i4_cv1/`、`runs/cv_summaries/mclr_bloom_stable_i4_cv1/`。

决策：淘汰 stable-TabM。它虽然收窄区间，但点误差、MedianAE、factor-of-2 和最差来源均显著退化，说明当前数据量下 TabM 没有可复现的增量信息。tree-only 在五折中的前四折均改善 `log1p MAE`、第五折持平，保留为候选。

反思：tree-only 第四折仍存在 AFT 无界外推——对真值 2200 的样本预测 40871，而对真值 11444 的样本只预测 99。现有协变量无法完全解释同一水体内的暴发幅度，同时原尺度算术融合会放大单专家爆点，因此进入 I5 检验融合几何，而不是继续增加神经网络容量。

## 10. I5：对数空间专家融合与 CQR 尺度消融

### 10.1 CQR 尺度

实现可选 `conformal_transform=log1p`，制品保存变换名称，旧制品缺少该字段时继续使用 identity。MC-LR I2 五折的无重训配对消融：未校准 coverage/宽度为 0.8062/58.6334，identity-CQR 为 0.8213/58.6351，log-CQR 为 0.8206/58.7486。多数折校准 adjustment 为 0，表明宽区间来自基模型和 stacking，而不是 CQR。

决策：能力和向后兼容实现保留，但当前正式配置仍使用 identity；log-CQR 不作为性能改进宣称。

### 10.2 OOF 一致的 log1p 融合

新增 `stacking_blend_transform`。选择 `log1p` 时，对 `(1 + concentration)` 做加权几何平均；distributional OOF 目标与推理使用完全相同的融合，不进行 test 后处理。配置：`mc_lr_bloom_v5_log_blend.json`。

| 五折均值 | I2 | I4 tree-only | I5 log-blend |
|---|---:|---:|---:|
| `log1p MAE` | 0.2680 | **0.2592** | 0.2603 |
| 原尺度 MAE (µg/L) | 63.6653 | 64.3918 | **62.2891** |
| MedianAE (µg/L) | 0.0824 | **0.0586** | 0.0619 |
| R² | -0.3044 | -0.4033 | **-0.1697** |
| Spearman | 0.5865 | 0.5877 | **0.5895** |
| factor-of-2 | 0.6434 | **0.6752** | 0.6648 |
| 最差来源 `log1p MAE` | 0.2927 | **0.2850** | 0.2861 |
| q90 tail `log1p MAE` | 1.4264 | 1.4325 | **1.4178** |
| q99 tail `log1p MAE` | 3.6931 | 3.6581 | **3.5900** |
| coverage | 0.8213 | 0.8128 | 0.8118 |
| 平均区间宽度 (µg/L) | 58.6351 | 47.0007 | **38.8638** |

结果路径：`runs/cv_summaries/mclr_bloom_logblend_i5_cv1/`。I5 相对 I4 的主 `log1p MAE` 仅退化约 0.4%，换来原尺度 MAE 改善 3.3%、q99 改善 1.9%、区间宽度改善 17.3%，并显著缓解最坏折的爆点。考虑预警系统对灾难性误报和无界外推敏感，I5 暂列综合候选；最终保留决策等待独立种子五折复核。

## 11. I5 独立种子稳定性与最终决策

服务器实验 `mclr_bloom_logblend_i5_seed1` 使用 `repeat_start=1` 和 seed stride 1009；该种子会重新分配水体组，因此比较五折分布而非逐折对号。

| 五折均值 | seed 42 | seed 1051 | 十折汇总 |
|---|---:|---:|---:|
| `log1p MAE` | 0.26026 | 0.26038 | 0.26032 ± 0.04724 |
| 原尺度 MAE (µg/L) | 62.2891 | 58.1547 | 60.2219 ± 35.9179 |
| MedianAE (µg/L) | 0.06186 | 0.06185 | 0.06185 ± 0.00957 |
| R² | -0.1697 | 0.0229 | -0.0734 ± 0.2901 |
| Spearman | 0.5895 | 0.5929 | 0.5912 ± 0.0767 |
| factor-of-2 | 0.6648 | 0.6639 | 0.6643 ± 0.0317 |
| 最差来源 `log1p MAE` | 0.2861 | 0.2944 | 0.2903 ± 0.0526 |
| q90 tail `log1p MAE` | 1.4178 | 1.3790 | 1.3984 ± 0.2373 |
| q99 tail `log1p MAE` | 3.5900 | 3.4110 | 3.5005 ± 0.9130 |
| coverage | 0.8118 | 0.8121 | 0.8120 ± 0.0498 |
| 区间宽度 (µg/L) | 38.8638 | 21.3381 | 30.1010 ± 31.2707 |

两个种子的主指标、MedianAE、factor-of-2 和 coverage 几乎一致，证明 I5 的收益不是随机初始化偶然。区间宽度和原尺度 MAE 的方差仍大，原因是不同水体折中极端 Uruguay ADDA-ELISA 记录的分配发生变化，而非数值训练不稳定。

最终决策：

1. `configs/mc_lr_bloom_v5_log_blend.json` 作为当前 MC-LR 综合冠军，适合风险预警与概率输出；
2. `configs/mc_lr_bloom_v4_tree_only.json` 保留为偏重近中位点误差/factor-of-2 的备选，不删除；
3. `stable-TabM` 淘汰，不进入默认部署；
4. total MC 使用 `configs/total_mc_bloom_v3.json`，但需继续做来源/检测方法分层；
5. static 模型只能低置信度 fallback；
6. 下一阶段最高收益预计来自严格分析物标签、同步现场特征和观测方法多任务建模，而不是继续堆叠更深架构。
