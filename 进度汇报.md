# model_redo 进度汇报

> 2026-06-07

---

## 一、旧项目 `model/` 分析

旧项目完成了完整的建模闭环：14 个数据集清洗 → XGBoost/LightGBM 集成训练 → 分类/回归评估 → SHAP 解释，产出 80+ 张评估图表。

**最佳结果**（Lake Erie，3,074 行）：分类 ROC-AUC 0.943，回归 R2 0.742，蓝藻叶绿素为最强特征。

**核心问题**：

1. **目标变量错误**：预测的是"总微囊藻毒素"（ELISA 总 MC），而非竞赛要求的 MC-LR。仅 EMLS 数据集有 MC-LR 专属字段，但该数据集未被用于训练。
2. **验证方式不可靠**：随机 80/20 划分，无时间外推或站点外推。
3. **HABs Training 回归效果差**：MICX 缺失率 66%，R2 仅 0.319，近半数预测偏离真实值 2 倍以上。
4. **集成策略粗糙**：等权平均，小数据集上集成反而劣于单模型。
5. **不可复现**：原始数据被 gitignore，模型产物未保留。

## 二、重构 `model_redo/` 贡献

重构项目的核心贡献是**发现了旧模型绕过的根本问题**，并建立了规范化的解决框架。

### 已完成

| 阶段 | 产出 |
|------|------|
| 审计旧项目 | 明确了旧模型目标变量混用（总 MC ≠ MC-LR）的根本缺陷 |
| 审计原始数据 | 17 个 CSV 字段级扫描，输出 `_audit_raw_csv_summary.json`，逐个识别 MC 字段类型 |
| 搜索外部数据 | 生成 GPT review packet，已下载 EPA NCCA 2015（含 MC-LR）+ NLA 2017 |
| 数据清洗 | 4 个 cleaned pkl（EMLS/Lake Erie/HABs NLA/NCCA 2015），统一列名和单位 |
| 项目规范 | CLAUDE.md 定义了审计优先、locked_test 验收、实验全记录、GPT 审查等 10 条规则 |

### 关键发现：MC-LR 数据瓶颈

审计发现本地数据中仅 2 个数据集含 MC-LR 专属浓度：

| 数据集 | MC-LR 有效样本 | 范围 | 问题 |
|--------|---------------|------|------|
| EMLS 欧洲湖泊 | 152 行 | 0–3.97 µg/L | 全部为欧洲湖泊，地理偏差大 |
| NCCA 2015 五大湖 | 53 行 | 0.1–1.04 µg/L | 缺少常规水质特征（无水温/pH/DO） |

合计约 205 个有效 MC-LR 样本，不足以建立稳健的回归模型。

### 尚未完成

- 无实验代码（cleaned pkl 的生成代码未保存）
- 无模型训练、无评估结果
- 外部数据下载不完整（NLA 2022、GLERL-CIGLR 待下载）

## 三、结论

重构在方法论上纠正了旧模型的根本缺陷（目标变量定义、数据审计缺失），但工程产出为零。项目卡在 MC-LR 数据不足的瓶颈。建议调整主任务为"总微囊藻毒素浓度回归"（可用样本 4,000+），MC-LR 作为二级分析，这是全球 HAB 监测的通行做法。
