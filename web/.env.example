# 默认使用前端 Mock service；连接真实后端时改为 false。
VITE_USE_MOCKS=true

# HTTP REST API 根路径，不要以 / 结尾。
VITE_API_BASE_URL=http://localhost:8080/api/v1

# WebSocket 实时事件地址。Mock 模式下不会建立连接。
VITE_WS_URL=ws://localhost:8080/api/v1/realtime
